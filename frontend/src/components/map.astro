---
import { onMount } from "solid-js";
import { initMap } from "../scripts/leafletmap.js";
import "leaflet/dist/leaflet.css";

onMount(() => {
  setTimeout(() => {
    initMap();
  }, 100); // Short delay to ensure DOM is ready
});
---

